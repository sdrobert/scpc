input_size: ${input_size}
hidden_size: 1024
vocab_size: ${vocab_size}
encoder_type: recur
transducer_type: encdec
stack: 3
recur_encoder:
  num_layers: 4
  recurrent_type: gru
  bidirectional: true
rwa_decoder:
  embed_size: 8
  hidden_size: 512
