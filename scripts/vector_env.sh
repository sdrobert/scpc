#! /usr/bin/env bash

# for n in {1..10}; do
#   [ -d "$HOME" ] && break
#   echo "HOME ($HOME) does not exist yet. sleeping for 15 seconds and retrying ($n/10)"
#   sleep 15
# done
source "$HOME/.bashrc"
conda activate scpc