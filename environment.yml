name: scpc
channels:
  - pytorch
  - nvidia
  - conda-forge
  - sdrobert
  - defaults
dependencies:
  - jiwer
  - nltk=3.5
  - pip
  - pydrobert-kaldi
  - pydrobert-param>=0.4.0
  - pydrobert-speech
  - pysoundfile
  - python
  - pytorch-lightning>=1.7
  - pytorch::pytorch-cuda>=1.10
  - requests
  - ruamel.yaml
  - sentencepiece=0.1.95
  - setuptools
  - setuptools_scm
  - pip:  # FIXME(sdrobert): Update to conda dep when pydrobert-pytorch releases >0.3.0
    - "git+https://github.com/sdrobert/pydrobert-pytorch.git"
